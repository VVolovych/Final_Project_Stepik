[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    need_review: marker for review
    login: marker for login
    debug: need to fix test
    login_guest: testing login form